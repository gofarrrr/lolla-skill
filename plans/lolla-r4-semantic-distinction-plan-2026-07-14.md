# Lolla R4 semantic-distinction plan

Status: provider-free goal complete; exact two-case holdout contract and runner
ready; no provider call or runtime integration authorized

Date: 2026-07-14

## Why this goal is earned

The corrected complementary-reader diagnostic resolved the operational
question. Four strict-JSON calls completed, compiled, and fanned in for an
exact `$0.010835`.

The semantic result was mixed in a diagnostically useful way:

- the target recovered one genuinely useful temporary-support and steady-state
  labor pressure;
- the same reader also relabeled an adopted contract precondition and existing
  privacy/renewal safeguards as new uncertainty;
- the restraint control relabeled an earlier matter and explicit six-month
  review benchmarks as new unresolved/reopen records;
- exact-ID relationships were mechanically sound but often restated those
  false-positive or already operationalized records.

The next defect is therefore semantic distinction, not token allocation,
schema transport, fan-in, graph recall, or model price.

## Goal

Design and validate a bounded probabilistic contract that distinguishes a
genuinely distinct remaining uncertainty from a condition, safeguard, process,
or review criterion already incorporated into the current position.

The job remains with the LLM because it requires interpreting messy language
across the whole conversation. Python may validate declared structure and
evidence, but must not decide meaning through keywords, chronology, or nested
gates.

## Required semantic distinctions

### Unresolved matter

Return a record only when a material question remains unanswered or unowned in
the final conversation state. Do not create one merely because:

- an action must occur before execution;
- the current position contains a contract term, written process, or operating
  condition;
- the matter appeared unresolved earlier but later evidence resolves or
  operationalizes it;
- the source describes risk in general.

### Reopen condition

Return a record only when later evidence or an event would materially reopen a
position that is otherwise current. Do not create one merely because:

- the position already schedules a review;
- a safeguard already says pause, stop, or do not renew;
- a metric or benchmark is already part of the adopted evaluation plan;
- the record paraphrases an operating requirement.

### Relationship

Return a relationship only when it adds inspectable meaning between exact
records. Completing with zero is correct when the only available statement
would paraphrase an endpoint or restate that a current position contains its
own safeguard.

## Provider-free work contract

1. Convert the corrected target and control outcomes into development fixtures,
   including explicit negative examples for adopted preconditions, safeguards,
   scheduled reviews, and later resolution.
2. Add at least these local cases:
   - genuine unresolved matter;
   - genuine reopen condition;
   - earlier gap resolved later;
   - adopted condition precedent;
   - existing pause/no-renew safeguard;
   - scheduled review with explicit benchmarks;
   - ambiguous evidence that must be preserved as ambiguous or quiet according
     to the schema contract;
   - useful exact-ID relationship;
   - zero relationship when endpoints merely coexist.
3. Refine the prompt contract and fixtures without adding a deterministic
   semantic classifier, a chronology gate, or a composite score.
4. Reuse the R3 reasoning-detail content-shape validator in any future runner;
   do not retain the broad R4 field-presence flag as the final custody rule.
5. Run focused and full provider-free tests.
6. Before any future provider validation, freeze a new source-first target and
   matched restraint holdout not used to tune the contract.

## Acceptance criteria

- positive fixtures preserve exact aliases and recover the intended distinct
  matter or reopen condition;
- adopted preconditions, safeguards, written processes, and scheduled reviews
  complete with zero unless a separate source-supported uncertainty remains;
- later evidence can resolve an earlier gap without Python inferring that
  resolution from turn order;
- relationship fixtures use exact admitted IDs and may complete with zero;
- ambiguous evidence is not forced into a supported record;
- no deterministic component judges relevance, semantic role, materiality, or
  relationship meaning;
- no runtime, graph, revised-answer, or receipt behavior changes;
- no provider call, model comparison, retry, fallback, or premium model use;
- no scalar quality score is created.

## Stop and decision boundary

Stop before a new provider call. A call becomes discussable only after the
provider-free contract passes and a new holdout is frozen under exact hashes,
with a separate costed authorization proposal.

This goal does not authorize integration even if all local fixtures pass. Its
purpose is to make the next causal experiment sharper and cheaper.

## Completion — 2026-07-14

The provider-free goal is complete:

- nine exposed development fixtures cover the seven uncertainty distinctions
  and two relationship behaviors above;
- the additive v2 prompt contract puts the full source before the final task,
  names positive and negative distinctions, and preserves zero and ambiguity;
- the v1 prompt and runner remain byte-frozen historical evidence;
- a new additive runner reuses deterministic assembly while applying v2
  relationship prompts, a 50 KB pre-transport relationship ceiling, and the
  strict R3 reasoning-envelope inspector;
- one unused false-stand-down target and one unused restraint control were
  frozen before request previews;
- the exact package rebuilds byte-for-byte and dry-runs without authorization
  or network access;
- the conservative maximum estimate is `$0.0280125` for at most four calls,
  below `$0.015` per case and `$0.03` total.

The only next decision is whether to authorize or defer that exact diagnostic.
It cannot select a production model, authorize wider-corpus work, or authorize
runtime or graph integration. See
`docs/conversation-understanding/lolla-r4-semantic-distinction-preparation-result-2026-07-14.md`.

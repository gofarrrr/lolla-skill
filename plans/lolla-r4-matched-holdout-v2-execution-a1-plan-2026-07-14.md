# Lolla R4 matched-holdout v2 execution A1 plan

Status: complete; one-use authorization consumed; no further execution authorized

Date: 2026-07-14

Canonical base: `b7d1d62c05bdf05f91401c25ceb0a2cc73ffe307`

Execution branch: `agent/r4-matched-holdout-v2-execution-a1`

## Falsifiable question

On the same new hidden long-form evidence, does the residual-task contract
improve false-positive restraint over the frozen v2 semantic-distinction
contract while preserving sensitivity to materially distinct residuals?

## Authorized boundary

The founder authorized one terminal execution attempt of the exact frozen
contract under these hard limits:

- eight provider calls maximum;
- `$0.03` provider-reported cost per matched case;
- `$0.12` provider-reported cost total;
- no automatic or manual retry;
- no semantic retry, fallback, healing, model substitution, relationship,
  evaluator, embedding, graph, pipeline, or runtime call;
- no request, prompt, schema, source, prior, seed, route, model, order, or
  output-allocation change;
- no publication, integration, R5 work, or product-usefulness claim.

The authorization was consumed as soon as provider transport attempted the
first authorized request. It cannot authorize a second run.

## Custody sequence

1. Verify canonical main, reviewed-package ancestry, contract and request
   hashes, source/prior and execution-visible hashes, target blindness,
   historical replay, dry-run zero, output-path absence, and key presence.
2. Recheck the exact model, Google route, required parameters, privacy
   controls, structured output, reasoning exclusion, usage custody, and price
   maximums against official Google and OpenRouter sources without modifying
   the frozen contract.
3. Create the local execution branch at canonical main.
4. Create and validate the exact one-use temporary authorization object.
5. Execute the fixed counterbalanced eight-call plan once, preserving the
   first raw terminal response bytes and stopping on the first failure.
6. Seal raw execution, authorization consumption, request/response hashes,
   usage, cost, provider attribution, and generation identities in a first
   local commit before opening protected evaluation evidence.
7. Open the protected source-first target only after the raw checkpoint,
   review every record and zero state without a provider evaluator, and apply
   the frozen non-scalar decision matrix.
8. Freeze the review, evidence manifest, closeout, documentation, replay
   exclusion, tests, and cold-start handoff in a second local commit.

## Terminal result

All eight calls completed once. Exact provider-reported cost was
`$0.01408165`. The raw evidence was committed before protected review in
`e2f83561686172538c8ac8876a53da2a804dc503`.

The residual arm preserved both genuine targets and removed some broad
inventory, but it failed both restraint controls. It repeated predefined
capacity thresholds, a scheduled board decision, a duplicate of the current
post-June gap, and a present gap inferred only from assistant suggestions.

Frozen completion decision:
`residual_task_repair_insufficient`

No retry, additional provider call, runtime/graph integration, R5 work, or
publication is authorized by this result.

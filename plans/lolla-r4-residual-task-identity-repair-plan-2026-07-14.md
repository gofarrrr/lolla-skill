# Lolla R4 residual-task identity repair plan

Status: complete provider-free; no holdout or provider authorization prepared

Date: 2026-07-14

Provider calls: 0

Provider cost: `$0.00`

## Falsifiable product question

Can Lolla construct one provider-ready paired contract whose complete
provider-visible job is residual discovery rather than broad inventory, while
preserving source and prior context, paired task shape, canonical custody,
relationship behavior, model, provider route, runtime, and graph boundaries?

This plan tests contract coherence only. It does not test whether a model
follows the contract.

## Single causal change

Change only the provider-visible semantic job identity:

- `residual_decision_gap` replaces the broad decision-gap label on the wire;
- `residual_reconsideration_dependency` replaces the broad reconsideration
  label on the wire;
- deterministic declared mapping restores those values to the existing
  canonical `unresolved_matter` and `reopen_condition` roles;
- the mapping reads only the declared enum and never examines interpretation,
  evidence, limitations, or other free text.

The paired response shape, record fields, two-record maximum, support and
outcome enums, zero and ambiguity states, evidence alias bounds, canonical
roles, relationship reader, model, provider route, runtime, and graph remain
unchanged.

## TDD slices

1. Specify residual-only role, task, vocabulary, schema descriptions, examples,
   and output rules with a failing public contract test.
2. Specify enum-only mapping plus separate zero and ambiguity compilation with
   a failing compiler test.
3. Project all nine historical development fixtures through a separate
   additive catalog and add full exposed Case 01/04 local expectations.
4. Build complete Case 01/04 request previews from the consumed A3 packet and
   request controls, then account for exact context order, hashes, bytes, and
   deterministic token estimates.
5. Reproduce historical v1/v2, target, authorization, execution, evidence, and
   relationship hashes; explicitly exclude the new downstream root from the
   frozen corpus replay.
6. Run focused and full provider-free verification, write the result, and
   update the restart-safe handoff.

## Stop rules

Stop instead of broadening if coherence requires changing source/prior
authority or order, splitting the paired task, changing relationship behavior,
adding governed-pending as an output surface, asking deterministic code to
infer operationalization, changing the model/runtime/graph, or making a
provider call.

No stop rule was triggered.

## Completion

The provider-ready contract is coherent within the authorized boundary:

- the complete provider-visible contract uses one residual ontology;
- the prompt is a new minimal module, not another warning appended to v2;
- provider labels map deterministically to the existing canonical roles;
- all seven historical uncertainty fixtures compile under projected residual
  labels, while both historical relationship fixtures remain unchanged;
- Case 04 locally expects zero on both residual surfaces;
- Case 01 locally preserves only the recurring operating funding/ownership
  residual and excludes the hydraulic/process false positive;
- full A3 source and prior context remain byte-identical and in the same order
  in both exposed previews;
- prompt text is 191 UTF-8 bytes shorter than v2 under the declared
  deterministic estimate, while the schema is 122 bytes larger for longer
  residual labels and exact dual-basis evidence wording;
- provider calls and cost remain zero, and no holdout was prepared.

Provider-free contract validity is not model semantic validation.

## Completion decision

`residual_contract_ready_for_new_holdout_design`

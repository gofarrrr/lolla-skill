# Lolla R4 separated-surface experiment pre-target design plan

Status: complete provider-free design; ready for a separate publication decision

Date: 2026-07-14

Canonical base: `1f092177670980963118c70b47097c379c3bbef9`

Local branch: `agent/r4-separated-surface-experiment-design`

Provider calls: 0

Provider cost: `$0.00`

## Falsifiable future question

Does asking for the two existing residual surfaces in separate provider calls
reduce unsupported opposite-surface companion records relative to asking for
both surfaces together, while preserving genuine findings?

This design does not answer that question. It freezes four authoritative
simulated conversations, four fallible priors, hash-bound human review, a
protected source-first target, exact paired and separated requests, a
target-blind future runner, and a non-authorizing experiment contract.

## Evidence boundary

Paired generation is not a general explanation for the A1 failures. The two
quiet controls failed without genuine companion findings, and the same paired
shape sometimes returned zero. The narrower unisolated pattern is that both
positive residual-arm cases also received an unsupported record on the
opposite surface.

The future causal intervention, if separately designed, is limited to task
shape:

- paired arm: one call reviews both existing residual surfaces;
- separated arm: one call reviews only the decision-gap surface and one call
  reviews only the reconsideration-dependency surface;
- paired output allowance: at most 1,600 tokens per case;
- separated output allowances: at most 800 plus 800 tokens per case;
- prospective maximum: four paired calls plus eight separated calls, twelve
  calls total.

Only the number of requested surfaces, number of returned reviews, number of
calls, singular grammar, and divided output allowance may differ. Complete
source and prior bytes, context order, residual ontology, evidence authority,
zero and ambiguity semantics, record fields and bounds, canonical role mapping,
model, provider route, privacy, reasoning, structured-output, streaming,
retry/fallback policy, runtime, graph, relationship reader, and compiler
behavior must remain unchanged.

Even if separation later removes companion records, persistent governed-
threshold or scheduled-decision errors still block integration. A successful
task-shape result cannot establish reader safety, model reliability, or real-
user usefulness.

## Source portfolio

Exactly four new 28-message alternating conversations are frozen:

1. volunteer cave-rescue equipment readiness: difficult governed-threshold
   control with named owners and adopted responses;
2. neighborhood observatory winter access: difficult scheduled-choice control
   with a fixed evidence window, authority, alternatives, and fallback;
3. relaxed-performance dance tour: one present recurring coordination and
   funding absence with no future premise-breaking dependency;
4. native-seed cryopreservation: a complete current plan with one conditional
   future event that ordinary outage machinery cannot absorb.

The role descriptions live only in this plan and the human review packet. They
must not appear in source or prior prose. The later protected target remains a
separate human judgment and may reject an intended role.

## Freeze order

1. Author four sources and four priors.
2. Validate message structure, aliases, prior structure, and prohibited text.
3. Freeze exact source/prior paths, hashes, bytes, counts, order, and zero-call
   custody in one manifest.
4. Prepare a complete human-review packet with every decision pending.
5. Record the founder's exact human pass declaration in commit `69d3026`.
6. Author and freeze the protected source-first target in later commit
   `740a525`, before any provider request existed.
7. Freeze the paired-versus-separated prompts, schemas, twelve requests,
   context and delta manifests, contract, target-blind runner, categorical
   evaluation matrix, current-practice record, and tests in a final local
   checkpoint.

No execution authorization, provider output, runtime integration, graph
change, relationship change, scalar score, or R5 artifact exists.

## Completed human gate

The founder supplied `human leakage review passes for R4 separated-surface
source freeze v1`. The declaration is bound to all eight source/prior hashes
and freeze-manifest hash in the additive custody record. Any change to those
bytes invalidates that review.

## Completion boundary

The paired arm makes one two-surface request per case at 1,600 output tokens.
The separated arm makes two single-surface requests per case at 800 tokens
each. All twelve request bodies and every allowed JSON-path delta are frozen.
The exact input duplication in the separated arm is declared. The future
runner requires a separate exact one-use authorization, never loads protected
evaluation evidence, preserves the first terminal bytes, and stops on the
first failure without retry, fallback, healing, substitution, or auxiliary
model calls.

Current authorization remains zero calls and `$0.00`. Publication and a later
one-use execution authorization are separate founder decisions.

#!/usr/bin/env python3
"""Build the provider-free museum fresh-context pressure packet."""
from __future__ import annotations
import argparse,hashlib,json,sys
from pathlib import Path
ROOT=Path(__file__).resolve().parents[2]
if str(ROOT)not in sys.path:sys.path.insert(0,str(ROOT))
from engine.system_b.canonical_model_selection import build_assessment_cards
from engine.system_b.fresh_reasoning_pressure import build_control_packet,build_control_prompts,build_packet,build_prompts,control_response_schema,response_schema
from engine.system_b.reasoning_process_contracts import schema_metrics
from engine.system_b.reasoning_process_views import canonical_json_bytes,sha256_bytes
def load(p):return json.loads(Path(p).read_text())
def sha(p):return hashlib.sha256(Path(p).read_bytes()).hexdigest()
def write(p,v):Path(p).parent.mkdir(parents=True,exist_ok=True);Path(p).write_text(json.dumps(v,indent=2,sort_keys=True)+"\n")
def main():
 ap=argparse.ArgumentParser();ap.add_argument("--output",type=Path,required=True);a=ap.parse_args();conversation_path=ROOT/"research/reasoning-process-position-role-first-v23-new-case-2026-07-12/case.txt";seal_path=ROOT/"research/minimum-viable-loop-mechanism-and-portfolio-seal-2026-07-12/result.json";kg_path=ROOT/"data/knowledge_graph.json";seal=load(seal_path);portfolio=seal["portfolios"]["museum_source_first"];cards=build_assessment_cards(load(kg_path)["models"]);refs=[{"path":str(p.relative_to(ROOT)),"sha256":sha(p)}for p in(conversation_path,seal_path,kg_path)];conversation=conversation_path.read_text();packet=build_packet(case_id="amb3-case04-museum-ai-license",conversation=conversation,portfolio=portfolio,challenge_cards=cards,source_refs=refs);control=build_control_packet(case_id="amb3-case04-museum-ai-license",conversation=conversation,source_refs=[refs[0]]);prompts=build_prompts(packet);control_prompts=build_control_prompts(control);ids=[x["model_id"]for x in packet["pressure_portfolio"]];pp=a.output.resolve()/"pressure-packet.json";cp=a.output.resolve()/"control-packet.json";write(pp,packet);write(cp,control);report={"schema_version":"lolla.fresh_reasoning_pressure_museum_pair_report.v1","status":"provider_free_fresh_pressure_pair_pass","arms":{"control":{"packet_path":str(cp.relative_to(ROOT)),"packet_sha256":sha(cp),"system_prompt_sha256":control_prompts["system_prompt_sha256"],"user_prompt_sha256":control_prompts["user_prompt_sha256"],"user_prompt_utf8_bytes":len(control_prompts["user_prompt"].encode()),"response_schema_sha256":sha256_bytes(canonical_json_bytes(control_response_schema())),"response_schema_metrics":schema_metrics(control_response_schema())},"pressure":{"packet_path":str(pp.relative_to(ROOT)),"packet_sha256":sha(pp),"candidate_count":len(ids),"candidate_ids":ids,"system_prompt_sha256":prompts["system_prompt_sha256"],"user_prompt_sha256":prompts["user_prompt_sha256"],"user_prompt_utf8_bytes":len(prompts["user_prompt"].encode()),"response_schema_sha256":sha256_bytes(canonical_json_bytes(response_schema(ids))),"response_schema_metrics":schema_metrics(response_schema(ids))}},"maximum_provider_calls":2,"provider_calls":0,"boundary":{"same_authoritative_conversation":True,"fresh_context_both_arms":True,"control_has_no_graph_candidates":True,"pressure_preserves_all_candidates":True,"runtime_effect":"none"}};write(a.output.resolve()/"report.json",report);print(json.dumps({"status":report["status"],"candidate_count":len(ids),"control_prompt_bytes":report["arms"]["control"]["user_prompt_utf8_bytes"],"pressure_prompt_bytes":report["arms"]["pressure"]["user_prompt_utf8_bytes"]},indent=2));return 0
if __name__=="__main__":raise SystemExit(main())

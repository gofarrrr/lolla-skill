# Decomposition design outcome

Status: material redesign required  
Date: 2026-07-11

This outcome follows the hash-locked design check in
`decomposition-design-check-2026-07-11.md`.

The article's small-job principle was validated at harvesting but not at fan-in.
Turn-pair calls were operationally and source reliable, yet three overlapping
lenses produced 88–95 events per fourteen-message case. Fresh global synthesis
received another overloaded context in a different form. After one generic
repair it still fragmented positions, over-cited thread evidence, omitted narrow
unresolved threads, and misclassified constraint source strength.

The deterministic layer worked and now fails closed when any synthesis family
is missing or ownership/trajectory evidence is inconsistent. Phase D did not
pass, so no reasoning abstraction, graph, or runtime integration was attempted.

Terminal evidence:
`research/conversation-event-a-e-conclusion-2026-07-11/decision.json`.

The next design question is bounded local semantic consolidation, not another
global prompt repair. Harvest families remain complementary lenses; any
consolidation must be probabilistic and preserve every input/disposition so
deterministic code never becomes a semantic relevance gate.

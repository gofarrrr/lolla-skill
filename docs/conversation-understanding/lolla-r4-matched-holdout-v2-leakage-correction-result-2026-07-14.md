# R4 matched-holdout v2 leakage-correction result

Status: complete provider-free; ready for a separate founder execution decision

Date: 2026-07-14

Provider calls: 0

Provider cost: `$0.00`

## Outcome

The corrected matched holdout is frozen without the v1 source/target leakage.
The expected outcomes must be inferred from distributed operational facts
rather than copied from experiment-specific labels or instructions in the
source or prior. The package is mechanically ready for a separate founder
execution decision; it contains no execution authorization and has not called
a provider.

This establishes provider-free evidence, contract, and custody validity only.
It is not model semantic validation, real-user evidence, runtime readiness, or
a product-usefulness claim.

## Rejected v1 evidence

Commit `b46464278e86f4c5d6c53e154bc272d93f09b116` remains the immutable rejected
v1 checkpoint. Its runner and custody mechanics passed, but its source and
prior text disclosed expected semantic classifications. V1 had no
authorization, made zero calls at `$0.00`, produced no provider output, and
must never receive founder execution authorization. The defect was evidence
design, not transport, schema, budget, or protected-target isolation.

Every v1 source, prior, target, contract, request, builder, runner, and manifest
remains byte-identical to that commit.

## Human-review custody

The exact declaration `human leakage review passes` is preserved verbatim and
bound to these source/prior hashes:

| Case | Source SHA-256 | Prior SHA-256 |
|---|---|---|
| `r4h2-case01-community-audio-archive` | `4af8f39ce9cc8e4b7edbb80111c2cfabac09037e176895ae380392308a4ac3c1` | `e77baaf2378d8cfc3cc29371b4dc5e472b585a09f29b29d8725ff49d99ae7095` |
| `r4h2-case02-serialized-essay-pilot` | `922228b8371d9536464adc402390f6e50d894927e0b9a7f9c60518d9a68bdb80` | `b5706dc359957e92fb25ee9535d3981835f7496fc4f138eae12885f18f3a3543` |
| `r4h2-case03-research-workspace-service` | `9c3c979fbe79e6a573f9dc316e1e03c7a1ffc29dc0b5abd7c139825ef2a652ad` | `53aff0c8c41fd7c1504718f4190a736addad67d0f150ddbaa6482cfb71c95e52` |
| `r4h2-case04-shared-language-course` | `ce8f1652612467e83589b9073b6a8c83273044fb4c5ab611852e1d916cdb0783` | `9e0ec28e5094b7c68560db1af1e231c6859972317a0dd2204cfa3914ad202ac5` |

All four human reviews are recorded as passed, and every case records that its
final four messages are insufficient to determine both surfaces. The
deterministic prohibited-vocabulary scan has zero matches, but is explicitly
not treated as semantic judgment.

Cases 01 and 02 end with summaries of several adopted documents and controls.
Those summaries provide some recency assistance. They do not disclose the
expected classification and are not independently sufficient to evaluate both
surfaces, so the holdout is not described as a pure test of recovering every
fact exclusively from distant context.

## Freeze order

The repository history preserves the required order:

1. `1d02d2abc1f416178fbd00a9f0b93aad353c24b2` — new v2 sources and priors;
2. `04706f67620b2548754454178a594d30228925ac` — hash-bound human pass;
3. `ac49ad2` — protected source-first target;
4. `742ec59` — matched request previews;
5. `524fe7d3109a21629458253e76713582d8ca9476` — exact deltas, contract,
   execution manifest, runner, pricing record, and replay exclusion.

The protected target SHA-256 is
`9630699de23f1782dc7761938d5912ec62ad12b64a2380678a3dffcfd11b3aa1`.
Its review record SHA-256 is
`f6ecbeced6d8ec29f44fbd5fe3a2035409abb3ae71366a6b25aaa619cc6960ce`.
The target existed before request previews, and no provider output existed at
any freeze stage.

## Source-first target

The complete sources support the intended two-control/two-positive pattern;
no case contradicted its intended experimental role.

| Case | `unresolved_matter` | Strongest aliases | `reopen_condition` | Strongest aliases |
|---|---|---|---|---|
| 01, community audio archive | quiet | `e009`, `e015`, `e019`, `e021`, `e025`, `e027` | quiet | `e013`, `e016`, `e019`, `e023`, `e027` |
| 02, serialized essay pilot | quiet | `e013`, `e019`, `e023`, `e025`, `e027` | quiet | `e015`, `e016`, `e017`, `e019`, `e021`, `e023`, `e025`, `e027` |
| 03, research workspace service | supported | `e007`, `e015`, `e017`, `e019`, `e025`, `e027` | quiet | `e009`, `e013`, `e021`, `e023`, `e025` |
| 04, shared language course | quiet | `e005`, `e007`, `e023`, `e025`, `e027` | supported | `e005`, `e007`, `e017`, `e023`, `e025`, `e027` |

Case 03 supports one recurring service-ownership, funding, and delivery-
capacity matter after the temporary launch support ends. The February proposal
date assigns recommendation work but does not itself provide a continuing
owner, appropriation, or capacity. Case 04 supports one premise-breaking State
Academic Board designation dependency: withdrawal would stop admissions
immediately, while the current program has neither local designation authority
nor a second designated instructor.

Cases 01 and 02 remain quiet because their pending questions are assigned to
named owners, signed procedures, thresholds, safeguards, fallback paths, and
scheduled decisions for the present horizon. The target preserves source
speaker ownership and modal force and does not infer that omitted detail proves
absence.

## Matched experiment contract

Arm A is the exact frozen v2 uncertainty reader and Arm B is the exact
published residual-task reader. Complete source and fallible prior bytes,
aliases, source → prior → task ordering, paired two-surface response shape,
model, Google Vertex route, per-case seed, 1,600-token output allocation,
minimal excluded-reasoning envelope, non-streaming behavior, strict JSON, and
privacy controls are equal within each case.

Only the declared residual-task intervention changes: provider-visible role,
task operation, surface vocabulary, schema name and semantic descriptions,
examples, evidence wording, and output rules. Each case has an exact delta
manifest; undeclared changes fail validation. Eight context manifests account
for every request component, complete source/prior inclusion, exact ordering,
task-at-end, schema context, sizes, hashes, declared omissions, equal fields,
and changed fields. No summary, chunking, relevance filter, chronology rule,
or deterministic semantic gate was introduced.

The frozen contract SHA-256 is
`92b7a52a3f05905b6b6ab2d45016d3cabf2841261ac41d8479d82477d9179a5f`.
The counterbalanced call order is Case 01 A/B, Case 02 B/A, Case 03 B/A, and
Case 04 A/B.

## Target isolation and runner custody

The runner source contains no target path, target hash, classifications,
leakage declaration, or protected-review reference. The execution-visible
manifest lists only source registries, packets, request previews, context
manifests, and matched-delta manifests. It contains no protected-review
reference and cannot be used to locate the target. The target and target-review
records are excluded from all provider-visible requests.

The future runner requires a separate authorization that exactly matches the
contract hash, all eight request hashes, fixed call order, call ceiling,
per-case and total cost ceilings, and every prohibited call class. It creates
network transport only after exact authorization and an explicit environment
file. Dry run constructs no transport. There are no automatic retries,
semantic retries, fallbacks, healing, evaluator calls, embedding calls,
relationship calls, graph calls, pipeline calls, runtime calls, or model
substitutions. Every terminal failure stops the run, and the first raw terminal
provider bytes are preserved exactly.

## Provider practice and budget

Official Google and OpenRouter documentation was rechecked on 2026-07-14. The
pinned future operator remains `google/gemini-3.1-flash-lite` through
`google-vertex` on OpenRouter, with fallbacks disabled, required-parameter
routing enabled, data collection denied, ZDR requested, strict structured
output, non-streaming transport, and minimal reasoning excluded from the
returned message.

At `$0.25` per million input tokens and `$1.50` per million output tokens, the
conservative full-allocation estimate for the eight frozen requests is
`$0.040521`. A future decision may impose `$0.03` per matched case and `$0.12`
total as anti-runaway ceilings. Those ceilings provide substantial margin but
still prohibit accidental loops, retries, or fallbacks. They do not authorize
the current package.

## Verification

The focused R4 slice passes 100 tests, including 22 leakage-correction and
runner tests. The historical replay validates exactly 12 cases, 543 case/
artifact links, and 400 unique frozen JSON artifacts. Builder validation and
the future runner dry run report zero calls and `$0.00`; fake transport covers
exact eight-call success and first-failure paths. Authorization, provider/model
identity, reasoning, schema, cost, budget, artifact tamper, target isolation,
and undeclared-delta failures are covered.

Changed JSON parses, changed Python compiles, and `git diff --check` pass. The
complete repository suite passes 4,928 tests and all 93 subtests. Its only
warning is the pre-existing `datetime.utcnow()` deprecation in
`scripts/stability_check.py`.

## Remaining unknowns

No model has seen these requests. The provider-free package cannot show that
Gemini follows the residual contract, resists the broad prior, uses late
evidence correctly, or preserves genuine residual sensitivity. Simulated
reliability cases also cannot establish usefulness on real founder
conversations. Those questions remain for a separately authorized execution
and later real-user evidence.

## Completion decision

`matched_residual_holdout_v2_ready_for_founder_authorization`

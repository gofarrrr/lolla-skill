# Lolla R4 residual-task identity repair result

Status: provider-free contract complete; no provider validation, holdout, or
runtime integration authorized

Date: 2026-07-14

Provider calls: 0

Provider cost: `$0.00`

Primary defect: Case 04 false-positive restraint

Regression constraint: Case 01 recurring operating funding and ownership

## Plain result

Yes: Lolla can express the next R4 reader as one coherent provider-ready
residual-discovery contract without changing the paired task, source/prior
authority, relationship reader, model, provider route, runtime, or graph.

The contract now asks the provider to account for the machinery already
adopted by the current position, subtract that machinery and the pending work
it governs, and emit only a materially distinct remainder affirmatively
supported by the source. This is the single semantic-task repair earned by the
causal diagnosis.

That is a contract result, not a model result. No model saw the contract. Local
fixtures and compilation prove that the object is internally coherent and
custody-safe; they do not prove semantic compliance, reliability, or product
usefulness.

## Provider-visible identity

Every provider-visible semantic field uses one residual ontology:

- `residual_decision_gap`;
- `residual_reconsideration_dependency`.

The role, three-step task, schema enum and descriptions, balanced examples,
evidence rule, zero rule, ambiguity rule, and output instructions all use
residual language. Neither broad canonical surface name is visible in the
prospective provider contract.

The semantic operation is:

1. account for what the current position assigns, governs, safeguards,
   conditions, schedules, or deliberately defers for the present horizon;
2. subtract that machinery and the pending work it governs;
3. return only a materially distinct remainder affirmatively supported by the
   source.

The contract states that pending is not residual, deferred is not unowned, a
scheduled decision can operationalize the decision work without selecting the
final outcome, adopted thresholds/fallbacks/exits/reviews are not themselves
new pressure, and omitted implementation detail is not affirmative evidence of
absence.

## Deterministic custody

The only new deterministic semantic-surface operation is the declared mapping:

| provider-returned surface | existing canonical internal role |
| --- | --- |
| `residual_decision_gap` | `unresolved_matter` |
| `residual_reconsideration_dependency` | `reopen_condition` |

The mapper reads the declared enum only. It does not inspect or classify
interpretation text, evidence, limitations, current machinery, materiality, or
operationalization. After mapping, the unchanged historical custody compiler
validates record shape, outcome consistency, exact aliases, counts, and
canonical reader roles.

The paired response still contains exactly two reviews, zero to two records
per review, the same four record fields, the same support/outcome enums, the
same exact evidence aliases, and separate quiet and ambiguous states.

## Evidence contract and exposed expectations

Every supported residual must cite exact source aliases that collectively
establish both:

- the material dependency or question;
- why the adopted current-position machinery does not fully handle it.

No deterministic strongest-evidence gate was added. Evidence remains available
for later source-first review.

The additive fixture catalog references the frozen historical nine-fixture
catalog by exact hash rather than rewriting it. Its seven uncertainty cases
are projected to residual provider labels and compile locally; its two
relationship cases remain hash-locked and unchanged.

The full exposed expectations are:

- Case 04 returns zero for both residual surfaces. Supplier transition work,
  quality/delivery/support thresholds, shared-interface and parallel-design
  work, redesign milestones, alternatives, fallbacks, and the fixed
  twelve-month decision remain governed pending work.
- Case 01 returns one `residual_decision_gap` for the recurring operating
  capability not assigned a recurring funder/owner. Its direct aliases include
  the one-time grant, unpriced shuttle staffing, under-attended staffing and
  deployment work, and city operations responsibility. The hydraulic no-go
  rule and adopted partial-deployment process produce no residual
  reconsideration record.

These are human-authored exposed development expectations, not provider
outputs.

## Full-context request custody

The prospective Case 01 and Case 04 previews were created by copying the
consumed A3 request bodies and changing only:

- system role and semantic contract;
- task surface vocabulary;
- schema name;
- schema surface enum and semantic descriptions.

Model, pinned provider route, seed, 1,600-token output cap, minimal reasoning
envelope, streaming setting, strict JSON mode, complete source, prior records,
and source→prior→task ordering are unchanged.

| case | source artifact SHA-256 | prior artifact SHA-256 | source context bytes | prior context bytes | schema bytes | conservative input estimate |
| --- | --- | --- | ---: | ---: | ---: | ---: |
| Case 01 | `04e787853c65c43dbca1c0d974a902fbb2bf70fabc90c533fd3e676ed910fecf` | `4ce920c93ce17cb37d4910ddb5b736700bf338abee06869f16827e9970be0263` | 27,859 | 2,270 | 1,775 | 17,668 tokens |
| Case 04 | `a5efa7240acc79742044ba3549c7963f4b4280be66294282cd4e855b0dedaa87` | `8a519bccda2c74351744ddd02105f61556a4d4aa95378876d72696c48ff02316` | 25,572 | 2,649 | 1,775 | 16,714 tokens |

The token figures use the declared deterministic conservative estimate
`ceil((message UTF-8 bytes + schema UTF-8 bytes) / 2)`. They are estimates, not
provider tokenizer results. Each per-case manifest also records every prompt
component, canonical request-body component, exact ordering, task-at-end,
complete inclusion, changed fields, unchanged dimensions, and declared
omissions.

## Prompt delta against v2

| component | v2 bytes | residual bytes | byte delta | estimated-token delta |
| --- | ---: | ---: | ---: | ---: |
| system prompt | 3,083 | 2,946 | -137 | -69 |
| user prompt | 30,668 | 30,614 | -54 | -27 |
| total prompt | 33,751 | 33,560 | -191 | -96 |
| response schema | 1,653 | 1,775 | +122 | +61 |

The prompt itself is shorter. The only material increase is the 122-byte schema
growth: the two residual surface identifiers are longer, and the evidence
description explicitly requires the material dependency/question plus the
reason adopted machinery does not fully handle it. Structure and bounds did
not grow.

## Frozen boundaries

- Historical v1/v2 prompt modules, runners, fixture catalog, targets,
  authorizations, request previews, execution output, evidence manifest,
  source-first review, and closeout reproduce under exact hashes.
- The existing relationship compiler remains
  `9253290093e62f62a9adbf8902ccf010ac4d4417c345222e4756e771496bf777`.
- The v2 prompt module, which owns the existing relationship prompt, remains
  `e774b19cd2bac461e6d586dffbde48515ab23d6f73e1eb158ed87bdcdccdf3c8`.
- Exact consumed relationship prompt hashes remain
  `31eb3f998acf6efffa0f8c510972eb1478d9b03938fd3c97f7c3d93d3d17625e`
  for Case 01 and
  `28d376509cf4bd1940ff42be5f46ff754a72702a8debe2518052130f5ce15617`
  for Case 04.
- The new downstream root is explicitly excluded from frozen replay discovery.
  Replay remains 12 cases, 543 case/artifact links, and 400 unique frozen JSON
  artifacts.
- No relationship change, governed-pending output, semantic classifier,
  provider runner, holdout, evaluator, embedding, retry, fallback, healing,
  runtime, graph, scalar score, R5 promotion, or product claim was added.

## Evidence package

- additive implementation:
  `engine/system_b/r4_residual_task.py`;
- provider-free builder:
  `scripts/evals/build_r4_residual_task_contract.py`;
- additive fixture catalog:
  `tests/fixtures/r4_residual_task/contract-fixtures-v1.json`;
- full contract, previews, compiled local expectations, context manifests, and
  exact manifest:
  `research/lolla-r4-residual-task-contract-2026-07-14/`;
- implementation plan:
  `plans/lolla-r4-residual-task-identity-repair-plan-2026-07-14.md`.

The exact artifact manifest contains 13 generated files and reports zero calls
and zero cost.

## Verification

- exact residual contract rebuild: passed at zero calls and `$0.00`;
- frozen v2 contract rebuild and consumed A3 execution closeout: passed;
- focused R4 slice: 59 tests passed;
- frozen corpus replay: exactly 12 cases, 543 case/artifact links, and 400
  unique frozen JSON artifacts;
- full repository suite: 4,887 tests and all 93 subtests passed in 336.90
  seconds;
- existing warning only: `datetime.datetime.utcnow()` deprecation in
  `scripts/stability_check.py:695`;
- changed JSON parsing and `git diff --check`: passed.

## Decision boundary

No stop rule fired: the residual job can be expressed without changing context
authority, task shape, relationship representation, state surfaces, model, or
runtime.

This result prepares no new holdout and requests no provider authorization. A
future holdout-design goal, if explicitly started, must create genuinely new
hidden evidence and preserve this exact single-variable contract. Provider-free
contract validity is not model semantic validation.

## Completion decision

`residual_contract_ready_for_new_holdout_design`

# Consumer-context role/attribution case candidate result

Date: 2026-07-23
Status: provider-free case candidate valid; execution not ready
Evidence class: retrospective mechanism-replay preparation, local structural
and custody evidence; not principal-human, provider, causal, usefulness, or
runtime evidence
Publication: [PR #385](https://github.com/gofarrrr/lolla-skill/pull/385)

## Outcome

One checked-in-safe case is now prepared against the six-cell consumer-context
contract. The selected context implementation is the honest prompt-level
role/attribution alternative: all future core cells must use the same endpoint,
while the prior answer is represented either as assistant-role history or as
explicitly attributed transcript evidence.

This does **not** simulate or claim an exact live host trajectory.

The generated package is:

`research/consumer-context-role-attribution-case-candidate-2026-07-23/`

Its canonical entrypoint is `manifest.json`; `readiness.json` is the
machine-readable stop/go record.

## Why this case

The retailer-pilot conversation is:

- checked in and safe to share;
- synthetic but realistic and fourteen messages long;
- already source-reviewed around a clear unresolved evidence problem;
- known to have produced a useful but answer-integrity-qualified fresh-pressure
  result.

That last property is both useful and limiting. It gives the mechanics a
meaningful pressure target, but it means this is a retrospective replay. It
cannot establish new prospective graph value, and prior exposure must remain
visible during human review.

## What the current graph does

The proposed reference condition starts with two fact-free lenses preserved by
the historical source-first target:

- `signaling`;
- `social-proof`.

The existing planner, unchanged, admits both directly and adds exactly one
outgoing one-hop target for each declared relation slot:

| Origin | Model | Slot |
| --- | --- | --- |
| direct | `signaling` | direct |
| direct | `social-proof` | direct |
| graph | `confirmation-bias` | antagonist |
| graph | `incentives` | tension |
| graph | `abstraction` | ally |

Six other eligible graph targets remain in reserve. Reserve is custody, not
semantic rejection.

The package reuses the published substrate reader, constitutional planner,
graph-survival serializer, complete prospective path-custody projection, and
existing pressure packet/prompt/schema compiler. No second compiler, reader, or
planner was added.

## What is mechanically proved

The deterministic validator establishes:

- exact source hash and complete fourteen-message custody;
- exact graph release and planner-policy identity;
- F2 and F3 use the byte-identical direct component;
- F3 and T3 use the byte-identical complete pressure presentation;
- F0 and T0 use the byte-identical explicit null-pressure block;
- every active planner pressure ID appears exactly once in the presented
  payload;
- no active pressure ID is missing, extra, merged, or duplicated;
- a six-cell order, single-draw policy, non-scalar review form, and blinding
  protocol are predeclared;
- provider calls and cost are zero;
- runtime and graph policy are unchanged.

These facts prove custody and experimental shape. They do not prove semantic
relevance, serious consideration, an answer improvement, or self-justification.

## The honest incompleteness

There are six request-preview slots, but only five have complete
provider-neutral message/schema envelopes.

F1—current live semantic bridge plus current graph—is explicitly blocked. No
checked-in current-live bridge output exists for this exact source. Producing
one would require a separately frozen and authorized semantic-supply call. An
older role reader, older mechanism reader, deterministic approximation, or R4
artifact would create a false comparison and a parallel system.

The other open gates are:

- the blank principal-human source-first target;
- principal-human approval, correction, or rejection of the proposed
  signaling/social-proof reference condition;
- exact provider/model/interface/generation settings;
- provider tokenizer counts and request cost;
- a maximum call and USD ceiling;
- exact founder authorization.

Therefore:

```text
design shape valid: yes
mechanical custody gates: pass
execution ready: no
causal interaction identified: no
self-justification identified: no
```

## Next decision

The next action is human and source-first, not a provider run.

A principal human should read only
`research/independent-phase5-cases-2026-07-12/useful-pressure-case.txt`, then
complete and sign the source-only part of
`principal-human-target-template.json` before opening the candidate identities,
graph paths, prior output, or request previews.

After that target is sealed, the reviewer can approve, correct, or reject the
proposed reference condition. Only then is it sensible to decide whether to
freeze F1 and a paid provider contract.

## Verification

Focused provider-free checks:

```bash
PYTHONPATH=. python3 scripts/evals/build_consumer_context_case_freeze_candidate.py --validate-only
PYTHONPATH=. python3 scripts/evals/validate_consumer_context_case_freeze_candidate.py
PYTHONPATH=. pytest -q tests/test_consumer_context_case_freeze_candidate.py
```

The final repository suite passed 5,083 tests and all 93 subtests. Its only
warning was the pre-existing `datetime.utcnow()` deprecation in
`scripts/stability_check.py`.

Provider calls: 0.
Provider cost: USD 0.00.
Private archives inspected: no.
Principal-human fields filled: no.
Runtime behavior changed: no.
Graph policy changed: no.

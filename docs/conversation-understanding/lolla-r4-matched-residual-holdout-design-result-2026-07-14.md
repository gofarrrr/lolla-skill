# R4 provider-free matched residual-contract holdout design result

Status: complete provider-free; frozen experiment not executed

Date: 2026-07-14

Provider calls: 0

Provider cost: `$0.00`

## Outcome

The matched design is coherent and ready for a separate founder decision. It
isolates the residual-task identity intervention against frozen v2 on the same
four new sources and fallible priors without changing task shape, source/prior
authority, context order, model, provider route, seed, reasoning envelope,
output allocation, streaming, strict-JSON policy, or privacy controls.

This result establishes provider-free design and custody validity only. It is
not model semantic validation, product usefulness evidence, or authorization
to execute.

## Publication gate

The prerequisite residual-task change was published through GitHub PR #364.
The PR head remained the founder-authorized commit
`b513686d00ff97d6d96003c2e4064cd18bd0d1bc`; no required check, requested
change, or unresolved actionable review thread blocked the normal merge.
Canonical `main` now contains it through merge commit
`06422338c3351753e487b70ca1c25ecc7c4a464c`. This goal began only after local
`main` matched that clean canonical state.

## New hidden simulated evidence

Four new 28-message conversations and their fallible priors are frozen under
exact hashes before any request preview or provider output:

| Case | Domain and experimental role | Source-first canonical expectation |
|---|---|---|
| 01 | Oral-history archive release; governed-pending control with a broad unresolved prior anchor | Both surfaces quiet |
| 02 | Independent press serialized-audio pilot; governed-pending control without a matching prior gap anchor | Both surfaces quiet |
| 03 | Research-data workspace stewardship; genuine continuing ownership/funding/capacity gap | `unresolved_matter` supported; `reopen_condition` quiet |
| 04 | Cross-campus language program; genuine external designation dependency not covered by current machinery | `unresolved_matter` quiet; `reopen_condition` supported |

The domains and vocabulary are structurally distinct from the exposed flood,
installation-grant, supplier/component, parallel-redesign, and twelve-month-
review examples. Each source has multiple decision threads, revisions,
qualifications, ambiguity, and material evidence in late messages. They remain
simulations, not real-user evidence.

The protected source-first target records, for both canonical surfaces in
every case, the expected supported/quiet disposition, exact strongest aliases,
why the matter is or is not outside adopted machinery, speaker ownership,
modal force, predicted broad-inventory false positives, limitations, and
ontology assumptions. It was authored after the source/prior hash freeze and
before request previews. It is absent from both provider requests and the
runner-visible execution manifest; the runner source contains no path or
reference through which it can load the protected evidence.

## Matched intervention custody

Each case has two provider-ready request previews:

- Arm A uses the exact frozen v2 uncertainty-reader prompt and schema.
- Arm B uses the exact residual-task prompt and schema from the published
  repair.

Both arms share byte-identical authoritative source and prior artifacts, exact
canonical source and prior context, source → prior → task order, model,
`google-vertex` route, fixed per-case seed, 1,600-token output cap, minimal
excluded-reasoning envelope, non-streaming policy, strict JSON, and privacy
controls. The paired two-surface task is not split.

The declared provider-visible deltas are limited to role, task operation,
surface vocabulary, schema name and semantic labels/descriptions, minimal
examples, evidence wording, and output rules. The schema delta is frozen to 13
exact JSON paths; any additional request difference fails local validation.

Eight context manifests record source/prior artifact and canonical hashes,
component hashes, UTF-8 bytes, deterministic token estimates, schema size,
full request estimate, exact section order, task-at-end, complete source
inclusion, fallible-prior authority, equal matched fields, declared semantic
deltas, unchanged dimensions, and omissions. No summary, chunking, relevance
filter, prior re-authoring, or deterministic semantic gate is present.

## Prospective execution envelope

The non-authorizing call order is counterbalanced and fixed:

1. Case 01 Arm A, then Arm B;
2. Case 02 Arm B, then Arm A;
3. Case 03 Arm B, then Arm A;
4. Case 04 Arm A, then Arm B.

The exact maximum is eight uncertainty-reader calls. Relationship, evaluator,
embedding, graph, pipeline, and runtime calls are all zero. Automatic retries,
semantic retries, fallback models, healing, and model substitution are all
zero or disabled. The runner stops after the first transport, provider
identity, usage/cost, reasoning-custody, terminal-status, schema/admission, or
budget failure.

The future runner is hash-frozen in the contract. It requires an authorization
artifact that exactly matches the contract hash, eight request hashes, call
ceiling, per-case ceiling, total ceiling, and all zero-call classes. It creates
the OpenRouter transport only after validating that authorization and loading
the explicitly supplied environment file. It preserves the first raw terminal
response—including HTTP error bytes—then records request/response hashes,
served model/provider, generation identity, complete usage, provider-reported
cost, and value-free reasoning-envelope custody.

Fake-transport tests cover all eight successful calls, first transport failure,
raw HTTP failure custody, provider identity, reasoning content, schema,
missing cost, excessive cost, exact authorization, request and artifact
tampering, and undeclared matched deltas. The dry run validates the contract
without constructing transport and reports zero calls and `$0.00`.

## Current official practice and budget

Official Google and OpenRouter documentation was rechecked on 2026-07-14 and
hash-frozen in the contract. Gemini 3.1 Flash-Lite remains compatible with
structured output and minimal reasoning; OpenRouter documents the pinned
provider, strict schema, seed, routing, privacy, usage, and cost controls used
here.

Using the published `$0.25` per million input-token and `$1.50` per million
output-token prices, the deterministic estimate assumes the full 1,600-token
output allocation on every call and no discount:

- conservative eight-call estimate: `$0.0424625`;
- proposed hard ceiling per matched case: `$0.015`;
- proposed hard total ceiling: `$0.06`.

The estimate and ceilings do not authorize or request spending.

## Source-first evaluation vector

The package freezes ten separate dimensions without a scalar score:

- mechanical execution and exact provider attribution;
- false-positive restraint;
- genuine residual sensitivity;
- zero-versus-ambiguity behavior;
- evidence precision;
- semantic surface placement;
- speaker/modal fidelity;
- prior-anchoring resistance;
- long-context/late-evidence use;
- operational cost and custody.

The categorical matrix distinguishes support for residual task identity,
non-discrimination, overcorrection, insufficient repair, regression, and
mechanically non-evaluable execution. Mixed findings cannot be averaged into a
composite result.

## Historical and provider-free boundary

- Historical v1, v2, and residual modules and both response schemas reproduce
  their exact frozen hashes.
- The new source-freeze and contract roots, contract, and protected target are
  explicitly excluded from corpus replay discovery.
- Frozen replay remains exactly 12 cases, 543 case/artifact links, and 400
  unique frozen JSON artifacts.
- No relationship prompt, relationship compiler, runtime, graph, model, or
  provider route changed.
- No provider output exists for these cases; no authorization artifact exists.
- Provider calls made and currently authorized are exactly zero at `$0.00`.

## Verification

The focused R4 design, replay, residual, and semantic-distinction slice passes
78 tests. The builder validates byte-for-byte, the future runner dry-runs with
zero calls, and the historical replay validates at 12/543/400. Changed JSON
parses, changed Python compiles, and `git diff --check` pass. The full repository
suite passes 4,906 tests with all 93 subtests passing. The only warning is the
pre-existing `datetime.utcnow()` deprecation in `scripts/stability_check.py`.

## Completion decision

`matched_residual_holdout_ready_for_founder_authorization`

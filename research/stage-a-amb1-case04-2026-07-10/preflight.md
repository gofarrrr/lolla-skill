# Ambiguous development Case 04 Stage A preflight

Date: 2026-07-10  
Status: frozen and authorized for one orchestrator invocation

This is a same-session synthetic development observation, not a clean holdout,
product validation, or answer-quality experiment.

The source is the next case in the rank frozen before authoring. Its capture
wrapper adds only declared message counts and preserves the semantic source
byte-for-byte. The Stage A dry run verifies the header, all 14 message markers,
and the exact bundled Python 3.12.13 executable before provider I/O.

The contract freezes one fresh extraction and the full private pressure
pipeline, OpenRouter Gemini 3.1 Flash Lite with reasoning disabled, direct
OpenAI-only embeddings, no revision, no experiment retry, exact hashes, source
red lines, outer timeouts, at most 36 OpenRouter calls, at most eight direct
OpenAI calls, and a $0.15 estimated cost ceiling.

Pipeline completion will establish operability only. A separate source-first
review must reject repetition, forcing, unsupported interpretation, and process
bloat before any candidate pressure receives development credit.

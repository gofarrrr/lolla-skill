# Accountability and joint-process cycle — notes

Status: active

## Goal

Harden exact pressure identity from handoff through disposition and receipt,
clarify routing as unresolved joint-process reasoning, and run one new paired
holdout plus new invariance fixtures only after offline gates pass.

## Structural custody hardening

- Paired downstream contracts may now declare exact treatment-only pressure-ID
  custody. Every frozen pressure ID must be an allowed value, appear exactly
  once in packet order in treatment, and remain absent from control.
- Control prompts do not reveal treatment pressure IDs.
- The live private-table ledger now validates the complete copied skeleton
  identity and disposition-effect field requirements, not only `source_id`.
- A research pressure handoff now has a one-to-one disposition-ledger skeleton
  and validator keyed by exact `pressure_id`.
- Independent semantic-effect review is a separate status with reviewed-output
  hash. Structural code never infers that the claimed effect matches prose.
- Combined focused custody suite passed before the new evaluation freeze.

## Joint-process target

The new target is an unresolved weakness after the complete conversation, not
any actor-local pattern that appeared temporarily. Every controlled mechanism
receives exactly one LLM-owned status: unresolved, resolved in conversation,
ambiguous, or not observed.

- unresolved routes to active deterministic seeds;
- ambiguous remains in the edge reserve without active seeds;
- resolved and not observed remain audit-only;
- `other_review_required` remains a manual-review reserve and never receives
  an automatic seed;
- facts and actor evidence remain in the audit packet but outside the sealed
  routing projection.

Batch 3 remains frozen and is not reused. The new fixtures use different
domains and wording.

## Joint-process invariance freeze

- contract SHA-256:
  `6da68354c7fe86dbfb9c03b039ae8f58e1ccc783dcd1577ca550596ddb3390b2`;
- runner SHA-256:
  `9c08ba58322bdbe2c93db784aaf3b752bb12477c1a27c91d9a1cc77e35c3fe65`;
- engine SHA-256:
  `9383ef614a7aff703b57955a8e596923c371520d6914e73820f072c44c6af279`;
- shared system prompt SHA-256:
  `480dd17696d4b1bdae9627f4d474cbdf353c1f6d5b819abae8aa88f51a20827b`;
- fixture prompt hashes: `facts_c`
  `16bdfe9eda525b553f4f6347a5c3d9a18a49d891423bb247486826609338230e`,
  `facts_d` `dc1d44057d0c18cb6d7e995bcc3bfbee48a81e35d803394d2d18066bee5d0bf5`,
  and `reasoning_repaired`
  `4a505cd0cfa4c36b8029a4c4d42ad3dff0ffa6b42096462971d05df9790e7623`;
- exactly three generation calls, zero evaluator calls, zero automatic retries;
- fixtures, target, routing map, V60 affordances, engine, runner, expected
  statuses, comparisons, and cost ceiling frozen before calls;
- no post-result prompt, fixture, ontology, or scorer repair.

## Joint-process invariance result

- all three outputs were valid and covered all ten mechanisms;
- fact-leak, cost, retry, and runtime gates passed;
- same reasoning/different facts produced identical routing projections and
  identical active/edge candidate sets;
- the repaired reasoning produced no active seed candidates;
- the frozen contract failed because one repaired status was `not_observed`
  rather than the required `resolved_in_conversation`;
- source review classifies that difference as a defensible F8 gold/scorer
  mismatch: the assistant gated the user-stated constraint immediately, so the
  failure may never have appeared;
- no gold change, prompt repair, or rerun;
- pattern-path runtime promotion remains blocked;
- the independent paired downstream holdout may proceed because exact
  pressure-custody offline gates passed and it does not consume this routing
  path.

## New paired holdout selection and Stage A freeze

Selected `case-02-multi-offer-career`.

Why:

- lower immediate safety exposure than whistleblower, parenting, property, or
  high-stakes medical cases;
- not previously used in the paired downstream program, while historical
  semantic-corpus use is disclosed;
- the existing answer is substantial enough for a strong fresh control;
- plausible non-obvious pressure concerns false option collapse, asymmetric
  family subsidy/capacity, unsupported future-optionality claims, reversible
  experiments, and headline startup/equity uncertainty;
- the treatment must not force the startup, invent a fourth option, treat
  spouse consent as a checkbox, or substitute another unsupported career
  narrative.

Stage A is frozen before fresh extraction. Historical extraction is rejected;
30 source messages, exact quote custody, separate core/Bullshit Index/OpenAI
call ceilings, direct-OpenAI-only embeddings, `$0.15` estimated cost ceiling,
and zero experiment retry are required.

Stage A contract SHA-256:
`c2c10ee0af1e7fec3d3feea1b51207faf1f94c0796d2584a27c03bbc0a751923`.

### Case 02 Stage A result

Stopped before pipeline. The extraction provider call completed, but the
output parent directory did not exist and the response failed to persist.
Usage metadata was lost with the artifact.

- no same-case retry;
- no pipeline or Stage B;
- classified as artifact-custody/operability failure, not semantic evidence;
- `run_extract.py` prospectively prepares or rejects output paths before any
  provider initialization;
- 48 focused extraction/skill tests passed after the repair;
- the next holdout must use a different case and the new runner hash.

## Replacement holdout selection

Selected `case-08-oncologist-career-family` under a new Stage A contract.
Selection occurred without observing Case 02 semantic output.

This is a career and household-capacity case with legal and professional-duty
edges treated only as verification targets. The predeclared incremental
pressure concerns compound NJ/Chicago/household load, claimed role leverage
versus actual authority, transition duty versus irreplaceability, and exit
options contingent on contract/institutional facts. The treatment must not
interpret law or professional duties, predict dementia or family outcomes,
force a decision, or invent an available reversible alternative.

Replacement Stage A contract SHA-256:
`c346dce52566b39b556089b82c7da1d310765f5a8c19ce145f355cf30e49609f`.

### Case 08 Stage A result

Stopped before pipeline. Capture passed at 18/18 messages with no truncation,
but the one allowed quote repair left two failures. Both preserved source words
and changed only internal phrase delimiters from double to single quotation
marks. The frozen gate still fails; no pipeline, Stage B, or same-case rerun.

Prospectively, the shared matcher now normalizes only quote delimiters while
preserving in-word apostrophes and returns the literal source substring. The
focused quote/extraction suite passed. After Case 02's persistence failure and
Case 08's quote failure, no third case is selected in this cycle: sampling
until admission would create selection bias and unnecessary calls.

## Cycle verification and decision

- full non-network suite: 3,967 passed, 1 skipped, 93 subtests passed;
- promote exact ID/skeleton custody, external effect-review status,
  output-path preflight, narrow quote-delimiter source recovery, and transitive
  hash locks;
- keep joint-process interpretation/routing, SK3 overlay, and portfolio
  expansion research-only;
- no new downstream pair exists, so no answer-utility or graph-value claim;
- next paid gate is one frozen extraction-admission smoke on a designated
  non-holdout, with persisted usage and matcher hash, before another holdout.

Cycle status: **complete**.

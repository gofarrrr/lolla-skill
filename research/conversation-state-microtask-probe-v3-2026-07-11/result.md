# Case 05 JSON-mode semantic baseline

JSON object transport solved the provider interoperability problem: all three
families reached inference, returned exact full source IDs, passed local typed
validation, and entered complete candidate custody without retries.

The semantic case nevertheless failed. The focal thread and its
`addressed_unresolved` disposition were recovered, but its latest trajectory
stopped too early. Constraints recovered only 2 of 9 strict reviewed items
(precision 0.50, recall 0.22) and strengthened two claims. The focal joint
position was again fragmented into separately owned pieces and lost assistant
Turn 7 as a qualification to the current direction.

The experiment program permits one generic semantic repair round. That repair
will be frozen before transfer to Cases 01 and 04; Case 05 will not be used to
claim transfer success.


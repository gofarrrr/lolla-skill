# Case09 preliminary novelty review

Status: **passed for one strong-control call**. This is a provisional trace review, not human ground truth and not proof that Lolla improves the answer.

The pipeline stayed within every frozen operational ceiling. It produced four pressures that are not merely extra model names and could change the decision process:

1. Replace the single 18-month checkpoint with staged evidence. Before formal commitment, require an early reversible test of real data access, data suitability, and method transfer. This follows the Optionality, Calculated Risk Taking, and Sunk Cost treatment contracts and the structural classifier's `covered_weak_threshold` finding.
2. Reverse the regret argument. The original answer uses anticipated regret to persuade toward Option 3. The Regret Theory affordance and its `regret-as-decision-rule` absence require regret of omission and regret of action to be checked against downside, probability, and reversibility.
3. Make the collaboration durable. Comparative Advantage changes the question from “can Dr. Silva provide data?” to “who owns what, which competence must the student build, and what survives if the postdoc or lab priorities change?”
4. Remove invented weight. The original advice contains unsupported job counts, success rates, timelines, and stakeholder psychology that make Option 3 sound more attractive than the known evidence allows.

All four have a plausible action or guardrail consequence. Therefore the predeclared minimum—two trace-supported pressures, one consequential—is met. A single fresh strong-control call is authorized. The portfolio-consumer call remains unauthorized until the control comparison.

Important limitation: one of the twelve compacted Bullshit Index evaluations returned an empty result. The run is correctly marked partial. The frozen contract did not define one post-processing failure as a stop condition, so this limitation is carried forward rather than used to move the gate after seeing the result.

Provider separation was also verified: five embedding calls and two query-expansion calls went directly to OpenAI using `OPENAI_API_KEY`; the OpenRouter key was used for the reasoning pipeline. With an empty OpenAI key, automatic embedding mode disables this layer rather than routing it through OpenRouter.

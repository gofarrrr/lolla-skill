# Pre-Step-6 Raw Artifact Consumption Readout

Date: 2026-05-16

Status: research comparison readout only. This does not change runtime behavior,
`SKILL.md`, `HOW_IT_WORKS.md`, default `/lolla`, product docs, Lane 1, V60, the
canonical knowledge base, or public output.

Related:

```text
research/pre-step6-raw-artifact-consumption-discipline-2026-05-16.md
research/pre-step6-comparison-subagent-readout-2026-05-16.md
research/pre-step6-comparison-fixtures/mother-deciding-address-year-20260430T113301Z.md
research/pre-step6-comparison-aggregate-readout-2026-05-16.md
research/pre-step6-raw-artifact-render-validation-slice-2026-05-16.md
```

## Question

After the subagent comparison, the live research question changed from:

```text
does reasoning_bundle.v1 help?
```

to:

```text
can Step 6 consume disciplined raw reasoning_artifact.v1 pressure directly?
```

This readout applies the raw consumption discipline to the three existing
fixtures plus one no-worker sentinel.

## Arms

```text
Arm A: current control summary
Arm B: raw reasoning_artifact.v1 specimens with explicit consumption discipline
Arm C: indexed reasoning_bundle.v1, optional challenger
```

For the no-worker sentinel, Arm C is expected not to run unless Arm B leaves
real clutter.

## Compact Result

| Case | Shape | Raw Discipline Result | Bundle/Worker Result |
| --- | --- | --- | --- |
| PhD direction choice | conflict / fallback viability | Raw artifacts preserve the two gates: fallback executability and Silva/data access. | Bundle not needed; subagent pass already found raw tied bundle. |
| Founder equity grant | duplicate / systems pressure | Raw artifacts can use systems pressure while demoting valuation/instrument duplicates. | Bundle not needed; subagent pass already found raw tied bundle. |
| Consultant whistleblower | hard boundary / misfit discard | Raw artifacts preserve counsel-first and no-investigation boundaries while keeping tripwires compact. | Bundle not needed; subagent pass already found raw tied bundle. |
| Mother address year | no-worker / calibration sentinel | Raw artifacts add instrument-trust and tripwire sizing while declining power-dynamics worker framing. | Worker should not run; bundle not needed. |

Aggregate:

```text
raw discipline beats current control in narrow missing-boundary spots: yes
raw discipline beats indexed bundle: no
raw discipline ties indexed bundle while simpler: yes
raw discipline declines one worker/lens: yes
bundle implementation authorized: no
worker orchestration authorized: no
```

## Case Notes

### Third-Year PhD Student

Raw artifact discipline says:

```text
Use phd_fallback_viability_boundary and phd_silva_constraint_retest.
Keep phd_base_rate_fit_gate qualitative.
Keep phd_quiet_option_expansion_caution quiet.
```

Final-answer lift over control:

- make the 18-month pivot conditional on the fallback still being executable;
- make Silva/data access a measured constraint test, not a hopeful dependency.

Why the bundle is not needed:

- the two controlling gates are visible in the raw artifacts;
- `risk_if_forced` prevents killing option 3 too early;
- `risk_if_ignored` explains why the fallback cannot stay vague;
- the quiet option-expansion artifact already tells Step 6 not to bloat.

Decision:

```text
prefer_raw_artifacts
```

### Founder Grant Marcus Equity

Raw artifact discipline says:

```text
Use the dependency-system artifact.
Use metric/staged-commitment pressure.
Demote valuation caveat and middle-instrument menu as checks.
Reject unsupported software-architecture diagnosis.
```

Final-answer lift over control:

- convert Marcus's demand into a dependency-system problem;
- require named measures before permanent equity/governance concessions.

Why the bundle is not needed:

- raw artifacts already carry priority hints, discard conditions, and
  risk-if-forced fields;
- careful Step 6 can suppress duplicates without an index.

Decision:

```text
prefer_raw_artifacts
```

### Mid-Level Consultant Report

Raw artifact discipline says:

```text
Preserve counsel-first.
Preserve no confrontation / no private investigation / no unusual access.
Use attorney-intake and Wednesday-protocol artifacts.
Keep leverage/power framing discarded.
Keep tripwires compact.
```

Final-answer lift over control:

- add a usable Wednesday interaction protocol;
- distinguish GC, audit committee, and regulator channels;
- test counsel incentives without choosing a channel prematurely.

Why the bundle is not needed:

- raw hard boundaries are explicit;
- `risk_if_forced` handles over-expansion;
- misfit discard can be followed directly.

Decision:

```text
prefer_raw_artifacts
```

### Mother Deciding Address Year

Raw artifact discipline says:

```text
Use mother_surveillance_instrument_trust_gate.
Use mother_commitment_sizing_tripwires.
Keep mother_duplicate_base_rate_caution quiet.
Discard mother_power_dynamics_worker_decline.
```

Final-answer lift over control:

- add that silence in phone surveillance is weak evidence, not reassurance;
- keep the slow-repair strategy bounded by professional guidance and tripwires;
- avoid turning general grooming facts into numeric claims.

Why a worker should not run:

- the remaining pressure is a narrow source/discipline issue;
- the power-dynamics lens is either not activated or duplicate of ex
  information-weaponization pressure;
- a worker would likely add strategic clutter to a safety/trust case.

Decision:

```text
prefer_raw_artifacts
decline_worker
```

## Raw Discipline Criteria

| Criterion | Result | Evidence |
| --- | --- | --- |
| Source-grounded force survives | pass | Each case uses only case-local artifact grounding. |
| Unsupported precision decreases | pass | PhD base rates and mother grooming/base-rate language stay qualitative. |
| Hard boundaries survive | pass | PhD fallback, consultant counsel/no-investigation, and mother tripwire boundaries survive. |
| Conflicts remain visible | pass | PhD ambition/fallback and consultant channel-choice tensions remain conditional. |
| Duplicates are demoted | pass | Founder valuation/options and mother base-rate pressure stay private/quiet. |
| Quiet artifacts do not bloat answer | pass | Quiet artifacts become checks, not public paragraphs. |
| Public prose avoids machinery terms | pass by contract | Raw discipline explicitly forbids machinery leakage. |
| Worker decline is possible | pass | Mother fixture records a worker/lens decline. |

## What This Supports

This readout supports:

```text
raw reasoning_artifact.v1 consumption discipline is the next smaller research path
```

It does not support:

```text
runtime changes
bundle implementation
worker implementation
product-doc changes
claims that raw artifacts solve all cases
```

## Next Implementation-If-Any Slice

If implementation begins later, start smaller than the bundle:

```text
reasoning_artifact.v1 render/consumption fixture schema
source_grounding / hard_boundary / discard_condition validation
public machinery leak check
local comparison runner
```

2026-05-16 follow-up: the first dormant render/validation fixture now exists at:

```text
scripts/research/pre_step6_raw_artifacts.py
tests/test_pre_step6_raw_artifacts.py
research/pre-step6-raw-artifact-fixtures/mother-address-year.raw-artifact-handoff.v1.json
```

It validates required fields, caps artifacts/source excerpts, renders the
private handoff under the raw cap, and checks public prose for machinery terms.
It does not authorize bundle runtime or worker orchestration.

Do not build `reasoning_workpack.v1`, worker prompts, or
`reasoning_bundle.v1` until raw artifact discipline fails a high-clutter case in
a way an index can plausibly fix.

## Decision

```text
proceed_with_raw_artifact_discipline_research
pause_bundle_runtime
pause_worker_orchestration
keep_step6_as_final_reasoner
```

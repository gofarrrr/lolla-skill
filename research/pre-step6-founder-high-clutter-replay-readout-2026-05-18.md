# Pre-Step-6 Founder High-Clutter Replay Readout

Date: 2026-05-18

Status: research-only high-clutter replay ledger slice. This does not change
runtime behavior, `SKILL.md`, `HOW_IT_WORKS.md`, default `/lolla`, product docs,
Lane 1, V60, the canonical knowledge base, or public output.

Related:

```text
research/pre-step6-semi-blind-comparisons/founder-grant-marcus-equity.high-clutter.semi-blind-comparison.v1.json
research/pre-step6-source-overclaim-audits/founder-grant-marcus-equity.high-clutter.rendered-hybrid.source-overclaim-audit.v1.json
research/pre-step6-replay-records/founder-grant-marcus-equity.high-clutter.off-default-replay.v1.json
research/pre-step6-hybrid-handoff-fixtures/founder-grant-marcus-equity.high-clutter.hybrid-handoff.v1.json
research/pre-step6-rendered-hybrid-answer-cores/founder-grant-marcus-equity.high-clutter.native.rendered-hybrid-answer-core.v1.json
scripts/research/pre_step6_replay_ledger.py
tests/test_pre_step6_semi_blind_comparisons.py
tests/test_pre_step6_replay_ledger.py
```

## Question

Can `card_first` plus quiet receipts carry a messy founder pressure bundle
without turning the final answer into a well-organized private notebook?

Mother tested overuse. Founder tests bloat and private mini-index drift.

The hostile condition:

```text
do not let "complete" beat "usable"
```

## Fixture

The existing high-clutter handoff uses:

```text
handoff_mode: card_first
source_pressure_card: dependency-system pressure
inspect_more: 1 contested false-precision item
quiet_receipts: 2 demotion receipts
```

Quiet receipts demote:

```text
duplicate middle-instrument catalog
misfit architecture diagnosis
```

## Semi-Blind Comparison

This slice used a local semi-blind rubric record, not a native judge.

Hidden map:

```text
A = raw-only
B = control
C = rendered hybrid
```

Criterion result:

```text
rendered hybrid wins: 4
control wins: 2
raw-only wins: 0
ties: 2
```

Aggregate:

```text
aggregate winner: C
unblinded: rendered hybrid
promotion_read: pass_to_replay
```

Control won:

```text
answer length / cognitive load
unforcedness
```

That matters. The control remains lighter and more conversational. Rendered
hybrid won because it preserved the most decision-critical clutter behavior:

```text
dependency-system pressure survives
full equity/title/board remains premature
serious staged partnership survives
vague delay / flat rejection risk survives
duplicate instrument catalog stays quiet
architecture diagnosis stays quiet
exit math stays false-precision caution
```

## Source/Overclaim Audit

Audit result:

```text
audit_result: pass
decision: counts_as_replay_win
naturalness_debt_level: medium
```

Required checks:

```text
source_grounding: pass
probability_overclaim: pass
evidence_gate_integrity: pass
unsupported_option_expansion: pass
naturalness_debt: watch
```

No overclaim findings were recorded.

The answer avoided:

```text
$9-13M reactivation
4-6x buyer multiples
technical architecture diagnosis
software architecture diagnosis
long phantom equity / revenue share catalog
private machinery leakage
```

## Bloat And Naturalness Debt

The source/overclaim audit marked naturalness debt as medium.

The rendered answer core is:

```text
1,499 chars
```

That is under the answer-core cap, but barely. The added detail remains
decision-relevant, yet this is a watch item because high-clutter cases are where
private receipts can quietly become a mini-index.

The replay record therefore marks:

```text
answer_bloat: watch
over_shaped_naturalness_debt: watch
```

This is a pass with two warnings, not a clean win.

## Replay Ledger Outcome

The replay ledger record is:

```text
comparison_decision: rendered_hybrid_wins
source_overclaim_audit_recorded: true
source_overclaim_audit_passed: true
replay_decision: pass_to_next_replay
product_promotion: blocked
naturalness_debt: medium
present_or_watch_failure_modes: 2
```

This is a replay win because the rendered answer wins the local comparison and
passes source/overclaim audit with non-high naturalness debt.

It is not product promotion.

## Interpretation

Founder high-clutter gives a narrower signal than it might look like.

Good signal:

```text
quiet receipts can keep duplicate/misfit artifacts quiet
dependency pressure can survive without a full raw dump
false-precision caution can survive without repeating the numbers
```

Live risk:

```text
rendered high-clutter answers can become long and framework-shaped
```

This result supports continuing static replay, not building a generator or
runtime integration.

## Decision

```text
founder_high_clutter_replay_passes_with_watch
quiet_receipts_stayed_quiet
dependency_system_pressure_survived
exit_math_stayed_false_precision_caution
instrument_catalog_did_not_reappear
architecture_diagnosis_did_not_reappear
naturalness_debt_medium
answer_bloat_watch
product_promotion_blocked
runtime_wiring_blocked
no_new_mode
no_bundle
no_workers
```

## Next Gate

Do not add more handoff design from this result.

The next useful move is an aggregate replay-ledger readout across the three
static records:

```text
PhD conflict: pass, medium naturalness debt
Mother quiet: pass, low naturalness debt
Founder high-clutter: pass, medium naturalness debt + bloat watch
```

That readout should decide whether we need one more negative/control replay or
whether the static evidence is enough to design a small replay generator
proposal. Product/runtime promotion remains blocked either way.

# Gate 6 current-case candidate inventory

Status: **complete; no paid graph-ablation candidate found**  
Date: 2026-07-10

## Simple result

We checked the six comparable July cases. None has the full chain needed for a
fair graph-specific paid test.

This is not “the graph failed.” It is “the current evidence does not isolate
the graph.”

## Why each case stops

- **Case 03:** passed Stage A and a paired test, but the complete companion
  graph surface was not preserved and its treatment packet has no exact graph
  lineage.
- **Case 05:** contains two graph chunks, but formal Stage A failed, no paired
  graph treatment exists, and the minor-safety case is outside the experimental
  risk scope.
- **Case 06:** passed a quiet pair, but its empathy edge is direct/V60 pressure
  and the complete graph surface was not preserved.
- **Case 07:** failed its operability gate and lacks a frozen graph treatment.
- **Case 09:** has Stage A and a strong control, but no complete graph surface or
  frozen downstream treatment.
- **Case 10:** preserves the full chain and three graph chunks, but its treatment
  cites none of them and source-first review admits zero.

Historical April/May scratch runs were not mined for a favorable exception.
Their architecture, prompts, and review contracts differ, so using them to
authorize a current paid test would be post-hoc.

## Custody repair completed

The provider-free shadow exporter now assigns exact identities such as:

```text
graph::regret-theory::ally::optionality::964793dc5498
```

Each identity retains hashes and source pointers while omitting raw chunk text
from the checked-in shadow. Case 10 produced three complete identities. Their
semantic disposition fields remain empty because identity is deterministic but
relevance is not.

This repairs the future evaluation prerequisite without changing the live
pipeline or sending more material to Step 6.

## Future paid shape

If a new holdout yields one source-supported graph-specific chunk, freeze three
arms before any call:

1. strong transcript reread;
2. non-graph portfolio using direct labels, frame routes, and capped embeddings;
3. the identical portfolio plus one exact graph pressure ID.

Use one call per arm, no retries, no model evaluator, and separate the public
revision from the private receipt. A shuffled-edge arm is deferred until a clean
graph-added signal exists.

## Decision

Paid Gate 6 remains blocked. The next useful product gate is Gate 7: test
whether a cold reader can reconstruct the Case 10 process, including its
negative result and repaired attribution mistake, without mistaking the receipt
for proof of quality.
